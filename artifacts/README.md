# Artifact Archives

Generated outputs previously stored at repository root are archived here.

- `debug/`: parser/renderer debug outputs and misc traces
- `test-runs/`: captured test run outputs

These are historical snapshots and are not consumed by app runtime.
